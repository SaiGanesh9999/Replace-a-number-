#include<stdio.h>
void main()
{
  int x,y,sum=0;
  scanf("%d",&x);
  while(x>0)
  {
    y=x%10;
    sum=sum+y;
    x=x/10;
  }
  if(sum%12==0)
  printf("Harsad Number");
  else{
   printf("Not Harshad Number");
  }
}
